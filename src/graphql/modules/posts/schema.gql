type Post {
  id: ID!
  title: String!
  content: String!
  authorId: User
}

input PostInput {
  title: String!
  content: String!
}

type Query {
  posts: [Post!]!
  post(id: ID!): Post!
}

type Mutation {
  postCreate(data: PostInput!): Post!
}